# MacBook Training Requirements
# Optimized for Apple Silicon (M1/M2/M3) and Intel Macs

# Core ML - Latest versions with MPS support
torch>=2.1.0  # MPS support for Apple Silicon
transformers>=4.36.0
datasets>=2.16.0
accelerate>=0.25.0

# Efficient Fine-tuning
peft>=0.7.0
trl>=0.7.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Evaluation
rouge-score>=0.1.2
evaluate>=0.4.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# Optional: Weights & Biases for tracking (comment out if not using)
# wandb>=0.16.0

# For Jupyter notebooks (optional)
# jupyter>=1.0.0
# ipywidgets>=8.1.0

# Note: We're NOT using bitsandbytes (not compatible with Mac)
# Instead using native PyTorch quantization and LoRA for efficiency